{
  "name": "postman-sync-action",
  "version": "0.0.0",
  "private": true,
  "description": "GitHub Action wrapper for @postman-sync/cli.",
  "license": "MIT",
  "scripts": {
    "typecheck": "echo 'no-op (shell + Dockerfile package)'"
  }
}
